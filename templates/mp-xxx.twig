{% extends "_base.twig" %}

{% block content %}
    <div class="ui orange inverted menu">
        <div class="header item">Detalhes do Meio de Pagamento</div>
    </div>

    <div class="ui breadcrumb">
        <a class="section" href="index.html">Início</a>
        <div class="divider"> / </div>
        <a class="section" href="mais.html">Mais</a>
        <div class="divider"> / </div>
        <a class="section" href="mp.html">Meios de Pagamento</a>
        <div class="divider"> / </div>
        <div class="active section">{{ nome }}</div>
    </div>

    
    <table class="ui striped table">
        <thead>
            <tr>
                <th>Mês</th>
                <th>Despesa</th>
            </tr>
        </thead>
        <tbody>
            {% for periodo, item in mp.periodos %}
                <tr>
                    <td>
                        <a href="{{ periodo }}.html">{{ item.mes }}</a>
                    </td>
                    <td class="right aligned">
                        {{ item.despesa|number_format(2, ',', '.') }}
                    </td>
                </tr>
            {% endfor %}
        </tbody>
        <tfoot>
            <tr>
                <th>Total</th>
                <th class="right aligned">{{ mp.total.despesa|number_format(2, ',', '.') }}</th>
            </tr>
        </tfoot>
    </table>
{%  endblock %}
